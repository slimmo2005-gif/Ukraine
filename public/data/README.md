# Bundled territory data

Filled by **Actions → Sync territory data and deploy** (git clone + copy, no GitHub API).

**Pages deploy:** Settings → Pages → **Deploy from a branch** → `gh-pages` / `/ (root)`.
