/** Public brand assets (served from /public/brand/). */
export const BRAND_NAME = 'Slim Analytics';

const base = import.meta.env.BASE_URL;

export const BRAND_LOGO_URL = `${base}brand/slim-analytics-logo.png`;
